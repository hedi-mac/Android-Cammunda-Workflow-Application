<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tn.scolarite.isi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeatherApp"
        android:usesCleartextTraffic="true">
        <activity
            android:name="tn.scolarite.isi.activity.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="tn.scolarite.isi.activity.Justificatif"
            android:exported="true" />
        <activity
            android:name="tn.scolarite.isi.activity.VerificationDemande"
            android:exported="true" />
        <activity
            android:name="tn.scolarite.isi.activity.MainActivity"
            android:exported="true">

        </activity>
    </application>

</manifest>